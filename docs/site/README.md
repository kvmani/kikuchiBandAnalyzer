# Sphinx Documentation Source

Build the documentation from the repository root:

```powershell
python -m sphinx -b html docs/site docs/site/_build/html
```

Open `docs/site/_build/html/index.html` after a successful build.
